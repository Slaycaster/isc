<?php

class Quaternary_unit_officesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('quaternary_unit_offices')->truncate();

		$quaternary_unit_offices = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('quaternary_unit_offices')->insert($quaternary_unit_offices);
	}

}
