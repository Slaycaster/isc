<?php

class Secondary_unit_officesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('secondary_unit_offices')->truncate();

		$secondary_unit_offices = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('secondary_unit_offices')->insert($secondary_unit_offices);
	}

}
