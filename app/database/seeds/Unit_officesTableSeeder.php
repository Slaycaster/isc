<?php

class Unit_officesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('unit_offices')->truncate();

		$unit_offices = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('unit_offices')->insert($unit_offices);
	}

}
