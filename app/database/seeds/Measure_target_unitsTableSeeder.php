<?php

class Measure_target_unitsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('measure_target_units')->truncate();

		$measure_target_units = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('measure_target_units')->insert($measure_target_units);
	}

}
