<?php

class Measure_targetsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('measure_targets')->truncate();

		$measure_targets = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('measure_targets')->insert($measure_targets);
	}

}
